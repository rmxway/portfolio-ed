"use client";

import { motion } from "framer-motion";
import styled from "styled-components";

/** Базовый `motion.section`: раскладка и анимации задаются в компоненте `Section`. */
export const MotionSection = styled(motion.section)`
  position: relative;
  width: 100%;
`;
